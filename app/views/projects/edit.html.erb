<h1>Edit</h1>

<%= form_for @project do |f| %>
<p>
  <%= f.label :title %><br>
  <%= f.text_field :title %><br>

  <!-- エラー存在チェック-->
  <% if @project.errors.any? %>
  <!-- エラー情報の習得-->
    <%= @project.errors.messages[:title][0] %>
  <% end %>

</p>

<p>
  <%= f.submit %><br>
</p>
<% end %>